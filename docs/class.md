
``` mermaid

classDiagram
    class کاربر {
        +شماره‌کاربری: عدد
        +نام: متن
        +ایمیل: متن
        +ورود()
        +خروج()
    }
    class کشاورز {
        +نام_مزرعه: متن
        +مدیریت_محصولات()
        +مشاهده_سفارشات()
    }
    class خریدار {
        +جستجوی_محصول()
        +ثبت_سفارش()
        +امتیاز_دهی_به_محصول()
        +ثبت_نظر()
    }
    class مدیر_سیستم {
        +بررسی_گزارشات()
        +نظارت_بر_تراکنش()
        +مدیریت_کاربران()
    }
    class محصول {
        +کد: عدد
        +نام: متن
        +دسته‌بندی: متن
        +قیمت: عدد
        +موجودی: عدد
        +افزودن_محصول()
        +ویرایش_محصول()
        +حذف_محصول()
    }
    class سفارش {
        +کد: عدد
        +وضعیت: متن
        +تاریخ: تاریخ
        +ثبت_سفارش()
        +لغو_سفارش()
    }
    class پرداخت {
        +کد: عدد
        +مبلغ: عدد
        +تاریخ: تاریخ
        +پرداخت_آنلاین()
    }
    class گزارش {
        +کد: عدد
        +محتوا: متن
        +تاریخ: تاریخ
        +ایجاد_گزارش()
    }

    %% روابط:
    کاربر <|-- کشاورز
    کاربر <|-- خریدار
    کاربر <|-- مدیر_سیستم
    کشاورز "1" o-- "*" محصول : مالک محصولات
    محصول "1" o-- "*" سفارش : محصول در سفارش
    خریدار "1" o-- "*" سفارش : ثبت کننده سفارش
    سفارش "1" o-- "1" پرداخت : عملیات پرداخت
    مدیر_سیستم "1" o-- "*" گزارش : ایجاد گزارش


    ```