
``` mermaid

stateDiagram-v2
    [*] --> ثبت_درخواست_سفارش : شروع فرآیند توسط خریدار
    ثبت_درخواست_سفارش --> در_انتظار_بررسی_کشاورز : سفارش ثبت شد
    در_انتظار_بررسی_کشاورز --> تایید_سفارش : تایید توسط کشاورز
    در_انتظار_بررسی_کشاورز --> رد_سفارش : رد توسط کشاورز
    تایید_سفارش --> پرداخت : منتظر پرداخت خریدار
    پرداخت --> آماده_سازی_محصول : پرداخت موفق
    پرداخت --> لغو_سفارش : عدم پرداخت/لغو توسط خریدار
    آماده_سازی_محصول --> ارسال_محصول : محصول آماده ارسال توسط کشاورز
    ارسال_محصول --> تحویل_به_خریدار : ارسال موفق
    تحویل_به_خریدار --> پایان : سفارش تکمیل و تحویل داده شد
    رد_سفارش --> پایان: فرآیند تمام شده (رد سفارش)
    لغو_سفارش --> پایان : فرآیند لغو سفارش

    note right of ثبت_درخواست_سفارش
      سفارش توسط خریدار ثبت می‌شود
    end note
    note right of در_انتظار_بررسی_کشاورز
      کشاورز سفارش را بررسی می‌کند: تأیید یا رد
    end note
    note right of تایید_سفارش
      سفارش پذیرفته و انتظار پرداخت
    end note
    note right of پرداخت
      پرداخت آنلاین توسط خریدار
    end note
    note right of آماده_سازی_محصول
      آماده‌سازی محصول توسط کشاورز
    end note
    note right of ارسال_محصول
      محصول ارسال می‌شود
    end note

```